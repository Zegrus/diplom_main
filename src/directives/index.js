import VFocus from "@/directives/VFocus";
import VIntersection from "@/directives/VIntersection";

export default [                                        // экспортировать будем массив и в этот массив будем экспортировать все директивы, которые есть в нашем приложении
    VFocus,                                             // делаем по сути тоже, что и с библиотекой компонентов
    VIntersection
]
